/** The capability that lets somebody read a Fleet's private roster. */
export const ROSTER_VIEW_CAPABILITY = 'roster.view';

/** The capability that lets somebody read every report of a Fleet in full. */
export const REPORTS_VIEW_CAPABILITY = 'reports.view';

/**
 * The capability that lets somebody change a Fleet's own settings, which is
 * the Owner's alone and includes who may see each report.
 */
export const SCOPE_SETTINGS_MANAGE_CAPABILITY = 'scope.settings.manage';
