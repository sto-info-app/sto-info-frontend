/**
 * The Fleet feature, as the site names it to a reader.
 *
 * Its own file rather than a constant on the section's landing page, because
 * the dashboard says it too. A page that imports one word from a lazily
 * loaded component pulls that component's whole chunk along behind it.
 */

/** The feature's name, worded as the rest of the site words it. */
export const FLEET_FEATURE_NAME = 'Fleet Community';
